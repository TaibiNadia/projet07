# config file for ansible -- http://ansible.com/
# ==============================================

[defaults]
inventory = ./inventory 

[ssh_connection]
ssh_args = -F ./ssh.cfg -o ControlMaster=auto -o ControlPersist=30